[project]
name = "openclaw-ecc-orchestrator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
ecc-orchestrator = "openclaw_ecc_orchestrator.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
